# Backend

Python service for:

- LoL Esports schedule/live polling.
- Model scoring.
- Probability history.
- Trade signal generation.
- Optional Telegram alerts.

The first implementation should reuse the existing local `lol_macro_backtest` model/scoring code rather than duplicating model logic here.
